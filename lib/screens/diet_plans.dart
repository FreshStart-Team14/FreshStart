import 'package:flutter/material.dart';

class DietPlansScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Personalized Diet Plans'),
      ),
      body: Center(
        child: Text('Get personalized diet plans here.'),
      ),
    );
  }
}
