import 'package:flutter/material.dart';

class SavedMoneyScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Saved Money'),
      ),
      body: Center(
        child: Text('View your saved money from not smoking.'),
      ),
    );
  }
}
